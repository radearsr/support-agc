<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("../partials/stylesheet"); %>
  <%- include("../partials/head"); %>
  <link rel="stylesheet" href="assets/css/pages/auth.css" />
  <title><%= title %></title>
</head>

<body>
  <div id="auth">
    <div class="row h-100 justify-content-center align-items-center">
      <div class="col-lg-5 col-12 shadow-lg">
        <div id="auth-left">
          <% if (statusCode === 200 && msg !== "") { %>
          <div class="alert alert-light-success color-success">
            <%= msg %>
          </div>
          <% } else if (statusCode >= 400) { %>
          <div class="alert alert-light-danger color-danger">
            <%= msg %>
          </div>
          <% }%>
          <h1 class="auth-title">Masuk</h1>
          <p class="auth-subtitle mb-2">
            Log in dengan data yang anda masukkan saat melakukan registrasi.
          </p>
          <form action="/login" method="post">
            <div class="form-group position-relative has-icon-left mb-2">
              <input type="text" name="email" class="form-control form-control-lg" placeholder="Alamat Email" value="radea@deyapro.com" />
              <div class="form-control-icon">
                <i class="bi bi-envelope"></i>
              </div>
            </div>
            <div class="form-group position-relative has-icon-left mb-2">
              <input type="password" name="password" class="form-control form-control-lg" placeholder="Password" value="radea" />
              <div class="form-control-icon">
                <i class="bi bi-shield-lock"></i>
              </div>
            </div>
            <button class="btn btn-orange btn-block btn-lg shadow-sm mt-2">
              Masuk
            </button>
          </form>
          <div class="text-center mt-5 text-lg fs-5">
            <p class="text-gray-600">
              Belum punya akun?
              <a href="/register" class="font-bold text-orange">Daftar</a>.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>